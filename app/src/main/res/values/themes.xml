<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.PlantSaver" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Kolor tła splash screena -->
        <!--<item name="windowSplashScreenBackground">@color/teal_700</item>-->

        <!-- IKONA: Tutaj wskazujesz swoją inną ikonę -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- Opcjonalnie: ikona "marki" na dole ekranu -->
        <!-- <item name="windowSplashScreenBrandingImage">@drawable/ic_branding_logo</item> -->

        <!-- Motyw, który zostanie zastosowany PO zakończeniu splash screena -->
        <item name="postSplashScreenTheme">@style/Theme.PlantSaver</item>
    </style>
</resources>